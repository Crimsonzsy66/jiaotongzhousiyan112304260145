# 交通标志检测实验报告

**姓名：** Crimsonzsy
**学号：** 112304260145

## 1. 实验目标
本实验使用 YOLO 模型完成交通标志目标检测任务，并对模型训练过程和检测结果进行分析。同时对训练代码进行优化重构，提升训练效率和代码可维护性。

---

## 2. 实验环境
- 操作系统：Windows 11
- Python 版本：3.12
- PyTorch 版本：latest (with CUDA support)
- YOLO 版本：Ultralytics YOLOv8
- 硬件环境：NVIDIA GeForce RTX 4060 Laptop GPU

---

## 3. 模型与训练设置
### 3.1 模型选择
本实验使用的模型为：
- 模型名称：YOLOv8s (Small Version)
- 选择该模型的原因：
  - 相比nano版本具有更高的检测精度
  - 相比medium/large版本训练速度更快
  - 在精度和效率之间取得了良好的平衡

### 3.2 训练参数
- 训练轮数（epochs）：100
- 图像尺寸（imgsz）：640
- batch size：默认
- 优化器：SGD
- 学习率：0.01 (初始), 0.01 (最终)
- 是否使用数据增强：是（Mosaic, MixUp, CopyPaste）
- 早停机制：patience=50
- 混合精度训练：启用（AMP）

### 3.3 训练命令
```bash
python train_model.py
```

### 3.4 代码优化
对训练脚本进行了全面的优化重构：
- **模块化设计**：setup_paths(), load_model(), get_train_config(), train_model(), evaluate_model()
- **参数精简**：删除35+个冗余默认参数，训练参数从99个减少到36个核心参数
- **性能优化**：workers从0提升到4，启用cache=True加速训练
- **日志系统**：使用logging模块替代print，添加时间戳和结构化输出
- **错误处理**：添加try-except块捕获训练和评估中的错误

---

## 4. 训练过程分析

### 4.1 损失曲线
在此插入训练损失图（如 `box_loss`、`cls_loss`、`dfl_loss` 等）。

![损失曲线](在此填写图片路径)

请结合图像回答以下问题：
1. 损失是否总体下降？**是的，训练损失持续下降，表明模型学习有效**
2. 哪一阶段下降最快？**训练初期下降最快，后期趋于稳定**
3. 后期是否趋于稳定？**是的，启用早停机制后，在最佳点停止训练**
4. 是否出现明显震荡或过拟合现象？**未出现明显过拟合现象**

### 4.2 评价指标变化
在此插入 `Precision`、`Recall`、`mAP50`、`mAP50-95` 等曲线图。

![指标曲线](在此填写图片路径)

请简要分析：
1. 哪个指标提升最明显？**mAP@0.5 提升最为明显，达到 94.43%**
2. 最终模型效果如何？**模型在验证集上表现优异，各项指标均达到较高水平**
3. 模型是否已经基本收敛？**是的，启用早停机制后，模型在最佳点停止训练，表明已收敛**

---

## 5. 混淆矩阵分析

在此插入混淆矩阵图。

![混淆矩阵](在此填写图片路径)

请重点分析：
1. 哪些类别识别效果最好？**红灯和停止标志识别效果最好，置信度高**
2. 哪些类别最容易混淆？**限速标志之间可能存在一定混淆，特别是相近速度值**
3. 造成类别混淆的可能原因是什么？
   - 限速标志在外观上相似度高
   - 部分图像中标志可能被遮挡或不完整
   - 拍摄角度和光照条件影响识别
4. 从混淆矩阵中可以看出模型还有哪些不足？
   - 小尺寸限速标志的检测可能需要加强
   - 复杂背景下可能出现误检

---

## 6. 检测结果分析

在此插入若干预测结果图（建议 2 到 4 张）。

![预测结果1](在此填写图片路径)
![预测结果2](在此填写图片路径)

请分析：
1. 哪些目标检测较准确？
   - 红灯、绿灯检测准确率高
   - 停止标志检测效果良好
   - 近距离的限速标志检测准确

2. 哪些图片中存在漏检或误检？
   - 小目标图片可能存在漏检
   - 复杂背景下可能出现误检

3. 误检、漏检的可能原因是什么？
   - 图像分辨率低导致小目标难以检测
   - 遮挡导致目标信息不完整
   - 光照条件差影响检测效果

4. 小目标、遮挡目标、远距离目标的检测效果如何？
   - 小目标检测效果有待提升
   - 遮挡目标检测存在一定挑战
   - 远距离目标检测效果相对较差

---

## 7. 提交成绩
- 本地验证结果：
  - mAP@0.5: 94.43%
  - mAP@0.5-0.95: 81.23%
  - Precision: 94.84%
  - Recall: 87.81%
- 比赛网站提交分数：待提交
- 当前排行榜名次（如有）：待排名

请简要说明：
1. 本地验证结果与提交分数是否一致？
   - 本地验证结果良好，预计提交分数与本地一致
   - 如有差异，可能是测试集分布与验证集不同导致

2. 如果不一致，可能原因是什么？
   - 训练集和测试集分布差异
   - 数据增强策略在测试集上泛化能力不足
   - 测试集图像质量或场景复杂度不同

---

## 8. 实验总结
请用简短几句话总结本次实验：
1. 本次实验中模型的主要优点是什么？
   - 检测精度高（mAP@0.5达94.43%）
   - 代码结构优化，可维护性强
   - 训练效率高，使用GPU加速

2. 当前模型最明显的问题是什么？
   - 小目标检测能力有待提升
   - 部分类别间存在混淆
   - 复杂背景下可能出现误检

3. 如果继续改进，你下一步会尝试什么方法？
   - 尝试更大模型（YOLOv8m/l）提升精度
   - 添加更多数据增强策略
   - 调整置信度阈值优化检测效果
   - 尝试模型集成策略

---

## 9. 代码优化总结
本次实验对训练脚本进行了全面的优化重构：
- **模块化设计**：将代码拆分为5个独立函数，提高代码可读性和可维护性
- **路径管理**：使用pathlib.Path动态获取路径，移除硬编码
- **参数精简**：删除35+冗余参数，训练参数从99个减少到36个
- **性能优化**：workers提升至4，启用cache加速训练
- **日志完善**：使用logging模块，添加时间戳和结构化输出
- **错误处理**：添加try-except块，增强程序健壮性

所有代码已提交至GitHub仓库：
https://github.com/Crimsonzsy66/jiaotongzhousiyan112304260145
